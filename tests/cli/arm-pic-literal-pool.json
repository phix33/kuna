{
  "schema": "re-probe/1",
  "probe_id": "a-fbaaba8ade8a",
  "kind": "cli",
  "cmd": [
    "{{KUNA}}",
    "strings",
    "{{BIN}}",
    "--json",
    "--filter",
    "kuna picpool prompt"
  ],
  "cwd": "{{WORK}}",
  "env": {
    "SLEIGHHOME": "{{SPECS}}"
  },
  "stdin": null,
  "timeout_s": 60,
  "repeat": 1,
  "target": {
    "binary_rel": "decompiler/crates/kuna-analysis/tests/fixtures/picpool_arm_le32",
    "binary_sha256": "1b83d695683e6b7c32b6596c2e61839d7c811828032744e05ca50be9b00b6825",
    "binary_size": 1297,
    "binary_source": "in-repo",
    "in_repo_path": "decompiler/crates/kuna-analysis/tests/fixtures/picpool_arm_le32",
    "selector": null,
    "selector_kind": "none"
  },
  "expect": {
    "exit_code": {
      "eq": 0
    },
    "stdout_is_json": true,
    "json": [
      {
        "path": "strings[0].address_hex",
        "op": "eq",
        "value": "0x300"
      },
      {
        "path": "strings[0].xrefs_count",
        "op": "gt",
        "value": 0
      },
      {
        "path": "strings[0].functions",
        "op": "len_gt",
        "value": 0
      }
    ]
  },
  "notes": "Promoted acceptance of arm-pic-literal-pool, re-pointed from the dataset witness (crackmes.one 68d40081224c0ec5dcedc2d2: 'Benar! Flag: ...' at 0x4ab reported xrefs_count 0 although main forms its address at 0x664 from the pool word at 0x6a0 plus the PC) at the vendored picpool_arm_le32 fixture -- CI has no dataset. Like the witness it is mapped under 0x1000. The refusals are in verify_picpool.rs."
}
